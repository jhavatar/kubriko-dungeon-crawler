kotlin.code.style=official

org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8

android.useAndroidX=true

# Suppresses warnings about iosSimulatorArm64 test tasks being disabled on
# this Intel (x86_64) host, which can't execute arm64 simulator binaries.
kotlin.native.ignoreDisabledTargets=true
